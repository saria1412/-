<!doctype html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#173a63">
  <meta name="description" content="نظام إدارة مخزون خام الورق المتزامن بين الجوال والكمبيوتر.">
  <title>سجل مخزون الخام | مصنع إبداعات دوما</title>
  <link rel="manifest" href="manifest.webmanifest">
  <link rel="icon" href="logo.svg">
  <link rel="apple-touch-icon" href="logo.svg">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;800;900&display=swap" rel="stylesheet">
  <style>
:root {
  --navy: #173a63;
  --navy-2: #0f2c4d;
  --navy-soft: #e8eef5;
  --orange: #f4821f;
  --orange-soft: #fdecdc;
  --cyan: #29abe2;
  --cyan-soft: #e4f6fc;
  --green: #218a52;
  --green-soft: #e5f6ec;
  --red: #c0392b;
  --red-soft: #fbe9e7;
  --yellow: #aa7410;
  --yellow-soft: #fff4d6;
  --bg: #f4f7fa;
  --card: #ffffff;
  --border: #dce4ec;
  --text: #1c2b3a;
  --muted: #66788c;
  --shadow: 0 4px 18px rgba(23, 58, 99, .09);
  --shadow-lg: 0 14px 40px rgba(15, 44, 77, .20);
}

* { box-sizing: border-box; }
html { color-scheme: light; }
body {
  margin: 0;
  min-height: 100vh;
  background: var(--bg);
  color: var(--text);
  font-family: "Tajawal", "Segoe UI", Tahoma, Arial, sans-serif;
}
button, input, select, textarea { font: inherit; }
button { -webkit-tap-highlight-color: transparent; }
[hidden] { display: none !important; }

.screen-center {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 22px 14px;
  background:
    radial-gradient(circle at 10% 10%, rgba(41, 171, 226, .12), transparent 34%),
    radial-gradient(circle at 90% 90%, rgba(244, 130, 31, .10), transparent 36%),
    var(--bg);
}
.auth-card, .status-card, .setup-card {
  width: min(100%, 470px);
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 22px;
  padding: 24px;
  box-shadow: var(--shadow-lg);
}
.auth-brand { text-align: center; margin-bottom: 20px; }
.auth-brand img { width: min(260px, 78%); height: auto; }
.auth-brand h1 { margin: 12px 0 4px; color: var(--navy-2); font-size: 23px; }
.auth-brand p { margin: 0; color: var(--muted); font-size: 13px; }
.auth-switch { display: grid; grid-template-columns: 1fr 1fr; gap: 7px; padding: 5px; background: var(--navy-soft); border-radius: 13px; margin-bottom: 18px; }
.auth-switch button { border: 0; border-radius: 9px; background: transparent; color: var(--muted); padding: 9px; font-weight: 800; cursor: pointer; }
.auth-switch button.active { background: #fff; color: var(--navy); box-shadow: 0 2px 8px rgba(23,58,99,.10); }

.field { margin-bottom: 13px; }
.field-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(210px, 1fr)); gap: 14px; }
label { display: block; color: var(--muted); font-size: 13px; font-weight: 800; margin-bottom: 6px; }
.req::after { content: " *"; color: var(--orange); }
input, select, textarea {
  width: 100%;
  border: 1.5px solid var(--border);
  border-radius: 10px;
  padding: 10px 12px;
  background: #fafcfe;
  color: var(--text);
  font-size: 14px;
}
input:focus, select:focus, textarea:focus { outline: none; border-color: var(--cyan); box-shadow: 0 0 0 3px var(--cyan-soft); }
textarea { min-height: 76px; resize: vertical; }
.help { margin-top: 5px; color: var(--muted); font-size: 11.5px; }
.error-text { min-height: 20px; color: var(--red); font-size: 12.5px; font-weight: 700; margin: 7px 0; }

.btn {
  border: 0;
  border-radius: 10px;
  padding: 10px 18px;
  font-weight: 800;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 7px;
  transition: .15s ease;
  text-decoration: none;
}
.btn:disabled { opacity: .55; cursor: not-allowed; }
.btn-block { width: 100%; }
.btn-main { background: var(--navy); color: #fff; }
.btn-main:hover:not(:disabled) { background: var(--navy-2); }
.btn-orange { background: var(--orange); color: #fff; }
.btn-orange:hover:not(:disabled) { background: #da7017; }
.btn-green { background: var(--green); color: #fff; }
.btn-red { background: var(--red); color: #fff; }
.btn-ghost { background: transparent; border: 1.5px solid var(--cyan); color: var(--navy-2); }
.btn-neutral { background: var(--navy-soft); color: var(--navy-2); }
.btn-sm { padding: 6px 11px; font-size: 12px; border-radius: 8px; }

.status-icon { width: 70px; height: 70px; display: grid; place-items: center; margin: 0 auto 12px; border-radius: 50%; font-size: 32px; background: var(--yellow-soft); }
.status-card { text-align: center; }
.status-card h2 { color: var(--navy-2); margin: 7px 0; }
.status-card p { color: var(--muted); line-height: 1.8; margin: 6px 0 18px; }

.app-wrap { max-width: 1180px; margin: 0 auto; padding: 18px 15px 60px; }
.stripe-bar { height: 8px; border-radius: 7px; margin-bottom: 13px; background: linear-gradient(90deg, var(--navy) 0 22%, var(--orange) 22% 44%, var(--navy) 44% 66%, var(--cyan) 66% 100%); }
.mast {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 17px;
  padding: 16px 22px;
  box-shadow: var(--shadow);
  display: flex;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
}
.brand { display: flex; gap: 15px; align-items: center; min-width: 250px; }
.brand img { height: 52px; width: auto; max-width: 220px; object-fit: contain; }
.brand h1 { margin: 0; color: var(--navy-2); font-size: 20px; }
.brand p { margin: 4px 0 0; color: var(--muted); font-size: 12.5px; }
.mast-actions { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
.sync-pill, .user-pill { border-radius: 22px; padding: 7px 11px; font-size: 12px; display: inline-flex; align-items: center; gap: 7px; border: 1px solid var(--border); background: #fff; }
.sync-pill { background: var(--cyan-soft); border-color: #99d9f1; color: 